'use client'

import { ResponsiveGridLayout } from 'react-grid-layout'
import 'react-grid-layout/css/styles.css'

// @types/react-grid-layout is v1 types but we use v2 — cast to any to avoid mismatched type errors
export default ResponsiveGridLayout as any
